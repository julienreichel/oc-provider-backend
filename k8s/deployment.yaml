apiVersion: apps/v1
kind: Deployment
metadata:
  name: oc-provider-backend
  namespace: oc-provider
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oc-provider-backend
  template:
    metadata:
      labels:
        app: oc-provider-backend
    spec:
      containers:
        - name: app
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
---
apiVersion: v1
kind: Service
metadata:
  name: oc-provider-backend
  namespace: oc-provider
spec:
  selector:
    app: oc-provider-backend
  ports:
    - port: 80
      targetPort: 3000
