apiVersion: apps/v1
kind: Deployment
metadata:
  name: oc-provider-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oc-provider-backend
  template:
    metadata:
      labels:
        app: oc-provider-backend
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: app
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: production
          envFrom:
            - secretRef:
                name: db
            - secretRef:
                name: config
