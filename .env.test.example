# ============================================================================
# LOCAL TESTING ENVIRONMENT TEMPLATE
# ============================================================================
# This is a TEMPLATE file for local development and testing.
# Copy this to .env (which is git-ignored) for local development:
#   cp .env.test.example .env
#
# These credentials match the oc-infra local setup and contain no secrets.
# Production deployments use Kubernetes secrets, never environment files.
# ============================================================================

# Local Database Configuration (matches oc-infra setup)
LOCAL_DB_NAME="db"
LOCAL_DB_USER="app"
LOCAL_DB_PASS="StrongLocalPass"
DATABASE_URL="postgresql://app:StrongLocalPass@localhost:5433/db?schema=public"
